package by.news.service.daojpa.repository.impl;

/*import by.news.service.daojpa.pojos.News;
import by.news.service.daojpa.pojos.Tag;
import org.springframework.stereotype.Repository;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
import java.util.List;
import java.util.Set;

@Repository
@Transactional
public class TagRepositoryImpl {

    @PersistenceContext
    EntityManager entityManager;

    public List<News> findByTag(String tagName) {
      String query = "SELECT t FROM t Tag WHERE t.newses.tags.tagName = sport";
        return entityManager.createQuery(query).getResultList();
    }

}*/
